import { unified } from 'unified'; import remarkParse from 'remark-parse'; import remarkMath from 'remark-math'; import remarkGfm from 'remark-gfm'; import remarkHtml from 'remark-html'; const text = 'Here is a math formula: (x) = |x|$ \\n\\nAnd a table:\\n\\n| A | B |\\n|---|---|\\n| 1 | 2 |'; async function run() { const file = await unified().use(remarkParse).use(remarkMath).use(remarkGfm).use(remarkHtml).process(text); console.log(String(file)); } run();
